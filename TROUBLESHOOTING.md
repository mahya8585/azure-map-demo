# トラブルシューティング

Azure Mapsデモで問題が発生した場合の確認事項です。

## キー未設定のメッセージが消えない

- [map-demo.html](map-demo.html)と[http-demo.html](http-demo.html)には、それぞれ独立した`AZURE_MAPS_KEY`定数があります。使用する両方のファイルを更新します。
- プレースホルダー`YOUR_AZURE_MAPS_SUBSCRIPTION_KEY`が残っていないことを確認します。
- 実キーをGitへコミットしないでください。

## 地図が表示されない、401または403になる

- `AZURE_MAPS_KEY`が正しいPrimary Keyへ置換されているか確認します。
- Azure Mapsアカウントが有効であることを確認します。
- キーに空白や引用符の重複がないことを確認します。
- ブラウザの開発者ツールで、CSPやネットワークによるブロックがないか確認します。

## 検索候補が自動表示されない

自動検索は3文字以上を入力してから350ミリ秒後に開始します。検索ボタンまたはEnterキーでは、1文字以上の検索語を実行できます。続けて入力すると直前の検索リクエストは中止されます。

## ルートを計算できない

- 通常のルート計算には2地点以上が必要です。
- 訪問順の最適化には、出発地・終点と2つ以上の中間地点を含む4地点以上が必要です。
- Route APIが`RoutePath`を返せない場合は、地点や回避条件を変更します。
- 高速道路、有料道路、フェリーの回避は完全除外ではありません。到達可能性によって回避対象を通る場合があります。

## ルート線の道路形状補正に失敗する

[map-demo.html](map-demo.html)はRoute APIの結果をSnap to Roads APIで補正します。補正に失敗した区間はコンソールへ警告を出し、Route APIが返した元の形状へ自動的にフォールバックします。ルート自体が表示されていれば操作は継続できます。

## 429になる

Azure Mapsの要求数制限に達しています。しばらく待って再試行し、Azure Portalで利用量とクォータを確認してください。

[map-demo.html](map-demo.html)のルート表示では、Route APIに加えてSnap to Roads APIを複数回呼ぶ場合があります。長いルートほどリクエスト数が増えるため、短いルートでも再現するか確認してください。

## http-demo.htmlが操作画面を表示しない

[http-demo.html](http-demo.html)は`file://`では初期化せず、HTTPサーバーが必要という画面を表示します。リポジトリのディレクトリで次を実行し、`http://localhost:8000/http-demo.html`を開きます。

```powershell
python -m http.server 8000
```

## 現在地を取得できない

- [http-demo.html](http-demo.html)を使用します。
- ページを`localhost`またはHTTPSで開きます。
- ブラウザとOSの位置情報権限を許可します。
- 企業ネットワークや端末ポリシーで位置情報が無効になっていないか確認します。
- `http://localhost`以外のHTTPホストはセキュアコンテキストにならないため、現在地を取得できません。その場合はHTTPSを使用します。

## 保存ルートが別ブラウザで見つからない

[http-demo.html](http-demo.html)の`localStorage`はブラウザとオリジンごとに分離されています。ホスト名やポートが変わると別の保存領域になります。

## 保存したルート線や所要時間が復元されない

保存対象はルート名、保存日時、地点名、座標だけです。ルート線、距離、所要時間は保存されません。「読込・再計算」で保存地点からRoute APIを再実行するため、キーとネットワーク接続が必要です。

## 保存件数が増えない

保存ルートは新しいものから最大20件です。21件目を保存すると最も古い項目が保存対象から外れます。ブラウザでサイトデータを削除した場合や、プライベートブラウズのセッションを終了した場合も保存内容が失われることがあります。
